{
  "name": "customerbase",
  "version": "1.0.0",
  "description": "Learn 'Simple GraphQL Server' from traversy media on YouTube",
  "main": "server.js",
  "scripts": {
    "dev:server": "nodemon server.js --exec babel-node --presets es2015,stage-2",
    "json:server": "json-server --watch data.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Yan Pengqiang",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-2": "^6.24.1"
  }
}
